name: data
version: 0.0.1
publish_to: none
description: Project data module

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  domain:
    path: ../domain
  retrofit: ^4.6.0
  drift: ^2.28.2
  drift_flutter: ^0.2.7
  path_provider: ^2.1.5
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  retrofit_generator:
  json_serializable: ^6.9.5

  build_runner: ^2.5.4
  drift_dev: ^2.28.0
